# VIM配置

**目录说明**

    .vimrc              # vim配置文件
    .vim                # vim plugin,color theme目录
        colors          # color theme(molokai,solarized,vivify)
        vim-go          # golang
        vim-markdown    # markdown
